/// Theme package for Supa architecture.
///
/// This package provides extended color schemes and token groups that extend
/// Flutter's Material Design theme system with additional semantic colors
/// for status indicators, tags, and UI components.
///
/// The main components are:
/// - [SupaExtendedColorScheme]: A theme extension that provides semantic color
///   tokens for various UI states (warning, success, error, etc.) and tag colors.
/// - [SupaExtendedColorTokenGroup]: A data class that groups related color
///   tokens (text, background, border) together.
library;

export 'supa_extended_color_theme.dart';
export 'supa_extended_color_token_group.dart';
