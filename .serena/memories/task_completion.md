## Before finishing tasks
- Run `dart format .` to format.
- Run `dart fix --apply` to auto-fix lints where safe.
- Run tests relevant to changes (e.g., `flutter test`).
- Update changelog if code modified.
- Check git status/diff before commit.
