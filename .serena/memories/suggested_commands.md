## Common commands
- Install deps: `flutter pub get`
- Format code: `dart format .`
- Auto-fix lints: `dart fix --apply`
- Run tests: `flutter test`
- Example app: from `example/`, `flutter run` (choose device, e.g., `-d chrome`)
- Lint check (if configured): `dart analyze`
