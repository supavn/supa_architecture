## Code style & conventions
- Use Dart/Flutter idioms with null safety.
- Run `dart format` before committing; keep code formatted.
- Apply `dart fix --apply` for lint fixes when possible.
- Follow architecture docs (modular, bloc/state management) in /doc.
- Update changelog when modifying code (per CLAUDE.md/AGENTS.md).
